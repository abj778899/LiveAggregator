name: Build IPA
on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        run: |
          ls /Applications/ | grep Xcode
          # 优先用最新的Xcode
          if [ -d "/Applications/Xcode_16.0.app" ]; then
            sudo xcode-select -s /Applications/Xcode_16.0.app/Contents/Developer
          elif [ -d "/Applications/Xcode_16.app" ]; then
            sudo xcode-select -s /Applications/Xcode_16.app/Contents/Developer
          elif [ -d "/Applications/Xcode_15.4.app" ]; then
            sudo xcode-select -s /Applications/Xcode_15.4.app/Contents/Developer
          else
            sudo xcode-select -s /Applications/Xcode.app/Contents/Developer
          fi
          xcodebuild -version

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode Project
        run: xcodegen generate

      - name: Fix project format compatibility
        run: |
          # 把项目格式版本降到Xcode 14兼容的格式
          sed -i '' 's/objectVersion = 77;/objectVersion = 56;/g' LiveAggregator.xcodeproj/project.pbxproj
          sed -i '' 's/objectVersion = 76;/objectVersion = 56;/g' LiveAggregator.xcodeproj/project.pbxproj
          sed -i '' 's/objectVersion = 75;/objectVersion = 56;/g' LiveAggregator.xcodeproj/project.pbxproj
          grep "objectVersion" LiveAggregator.xcodeproj/project.pbxproj

      - name: Build (Release, no code sign)
        run: |
          xcodebuild \
            -project LiveAggregator.xcodeproj \
            -scheme LiveAggregator \
            -configuration Release \
            -derivedDataPath build \
            -destination 'generic/platform=iOS' \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGN_ENTITLEMENTS="" \
            build

      - name: Verify .app exists
        run: |
          APP_PATH="build/Build/Products/Release-iphoneos/LiveAggregator.app"
          if [ -d "$APP_PATH" ]; then
            echo "App found: $APP_PATH"
            ls -la "$APP_PATH"
          else
            echo "App not found, searching..."
            find build -name "*.app" -type d
            exit 1
          fi

      - name: Package IPA
        run: |
          rm -rf Payload LiveAggregator.ipa
          mkdir -p Payload
          cp -r build/Build/Products/Release-iphoneos/LiveAggregator.app Payload/
          # 移除插件中可能导致安装失败的代码签名相关
          find Payload -name "_CodeSignature" -type d -exec rm -rf {} + 2>/dev/null || true
          zip -r LiveAggregator.ipa Payload
          ls -lh LiveAggregator.ipa

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: LiveAggregator.ipa
          path: LiveAggregator.ipa
          retention-days: 30
